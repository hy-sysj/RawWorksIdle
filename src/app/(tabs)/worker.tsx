import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function WorkerScreen() {
  return (
    <ScreenPlaceholder
      title="작업자"
      subtitle="가챠, 보관함, 광맥 배치를 관리하는 화면입니다."
    />
  );
}